'use client';

import MainDashboard from '@/components/dashboard/MainDashboard'; // now valid default
// OR if you prefer named: import { MainDashboard } from '@/components/dashboard/MainDashboard';
export default function Home() {
  return <MainDashboard />;
}